services:
  - type: web
    name: lottery-filters-1
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
    autoDeploy: true
